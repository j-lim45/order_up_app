import 'package:flutter/material.dart';

class ForgotPassword extends StatelessWidget {
  const ForgotPassword({super.key});

  Widget build(BuildContext build) {
    return Scaffold(body: Text('ForgotPassword class'));
  }
}